import 'package:flutter/material.dart';

class Users with ChangeNotifier
{
   void registerusers(String email, String password, String first_name, String last_name)
   {
      final String username = DateTime.now().toString();
      final String password = '';
      final String last_name = '';
      final String first_name = '';
      final String email = '';
   }
}